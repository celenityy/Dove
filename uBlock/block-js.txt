! Title: 🛡️ Block JS
! Version: 02March2025v1
! Expires: 12 hours
! Description: Block JavaScript to harden the privacy & security of Thunderbird.
! Homepage: https://dove.celenity.dev

! JavaScript has significant privacy & security implications, but blocking it typically causes significant breakage.
! This isn't really the case for Thunderbird though; we only need JavaScript for ex. certain email providers.
! It's typically preferable to block JS via uBlock Origin's `Disable JavaScript`, but we can't set this without locking it unfortunately.
! Blocking JavaScript via a filterlist like this allows users to override and re-enable it if desired, as well as control site exceptions, etc.

*##^script
